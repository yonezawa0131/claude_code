# CLAUDE.md

## モデル運用方針(Opus advisor + Sonnetセッション + Haiku scout)

このリポジトリでは、モデルを役割で使い分ける3層構成をデフォルトとする。

| 役割 | エージェント | モデル | 担当 |
|---|---|---|---|
| 頭脳 | advisor | Opus | 設計判断・行き詰まりの相談(読み取り専用) |
| 胴体 | メインセッション | Sonnet | **実装・編集・統合・最終判断はすべてここ** |
| 手足 | scout | Haiku | 読み取り専用の調査・一括チェック(並列可) |

- メインセッションは **Sonnet** で動く(`.claude/settings.json` の `model` で設定済み)
- 設計判断や行き詰まりでは、自分で押し切る前に **advisor サブエージェント(Opus)** に相談する
- 大量に読んで結論だけ欲しい調査は **scout サブエージェント(Haiku)** に委譲する

### advisor に相談する条件(いずれかに該当したら相談する)

1. アーキテクチャ・設計の判断で、実行可能な選択肢が複数あり優劣を決めかねるとき
2. 同じエラー・同じ失敗で **2回以上** 詰まったとき
3. 大規模リファクタや破壊的操作(削除・履歴改変・スキーマ変更など)に着手する前
4. ユーザーに提出する重要な成果物の最終レビュー

### 相談のしかた

advisor には次を圧縮して渡す: **目的 / 制約 / 検討済みの選択肢と失敗したこと / 判断してほしい点**。
返ってくるのは助言であり命令ではない。採否はメインエージェントが判断し、採用しない場合は理由を作業ログに一言残す。

### 相談しないこと

定型作業・単純な調査・1ファイルの小修正で advisor を呼ばない(コストと時間の無駄)。
迷ったら基準は「この判断を間違えるとやり直しコストが大きいか?」——大きければ相談、小さければ自走。

## scout(Haiku)の使い方

### scout に委譲する条件

1. **3ファイル以上をまたぐ調査**(使用箇所の洗い出し、命名規則・設定の実態調査、影響範囲の特定)
2. **機械的な一括チェック**(リンク切れ確認、規約違反の洗い出し、ドキュメントと実装の突き合わせ)
3. 調査の観点が複数あり独立している場合は、**観点ごとに scout を分けて並列にスポーンする**

### scout に任せてはいけないこと(ガードレール)

- **編集・実装は絶対に scout に出さない。** 書く仕事はすべてメイン(Sonnet)が行う。scout はツール権限上も書き込み不可
- **1タスクに複数の問いを詰め込まない。** 「1 scout = 1つの問い」。大きな調査は問い単位に分割して並列で投げる(Haiku のコンテキストは 200K と狭いため)
- **長い経緯や大量の前提が必要な調査は委譲しない。** 前提の説明が数段落で済まないなら、メインが自分で読む

### scout の報告の扱い(精度リスクの回避)

- scout の報告は**手がかりであって事実確定ではない**。編集の根拠にする箇所は、メインが該当ファイルの `path:line` を自分で開いて裏取りしてから使う
- 報告に含まれる「カバレッジ」(調べていない範囲)と「確信度」を必ず確認し、確信度が低い項目をそのまま実装判断に使わない
- scout が「編集・判断が必要」と差し戻してきたら、その仕事はメインまたは advisor に振り直す

## リポジトリ構成

- `.claude/agents/` — サブエージェント定義(advisor / scout)
- `.claude/skills/` — スキル(memory-dream / vault-compile / vault-synthesis)
- `.claude/hooks/` — SessionStart フック(個人スキル同期)
- `notes/` — プレイブック等のノート
- `experiments/` — 実験用ディレクトリ
- `scripts/` — ユーティリティスクリプト
