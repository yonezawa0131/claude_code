# CHANGELOG

## [1.2.1] - 2026-07-05

### Added

**複数ソース・複数ページの並列分析（slide-md-creator）**
- ソース（URL・画像・ファイル）が2つ以上ある場合、またはブランド理解のためにWebサイトの複数ページを読む必要がある場合に、Agentツールでサブエージェントを並列起動して分析するように
- 矛盾する値がある場合はより多くのソースで確認できた値を優先し、確認不能な項目はSTEP 3でユーザーに確認する

**sample.html生成仕様のさらなる洗練（slide-md-creator）**
- カード・サーフェスの背景色は白（#ffffff）を使わず、`--color-surface` / `--color-surface-soft`（Backgroundから算出）を使う方針に統一。ウォームトーンなデザインを保ちやすくした
- CSSカスタムプロパティに `--color-surface-soft` / `--font-body` を追加し、`--font-mono` はSLIDE.mdで指定されたモノスペースフォントを優先するように変更
- カラーパレット行を10〜12色程度に絞り込み、機能別グループ＋セパレーターで整理する仕様に変更
- Do/Don't欄のアイコン・背景色をSLIDE.mdのSuccess/Primaryカラーに応じて動的に決める方式に変更（固定の緑・赤だった仕様を廃止）
- 見出し・カード内フォントサイズを全体的に引き上げ（h1 54px→62px 等）、ページ4の箇条書きはカード数が少ない場合に横並びカードへ変える分岐を追加
- 灰色文字のハードコード（`#9AA0A6`）が5箇所残っていたのを `var(--color-text-muted)` に統一し、ダークモード対応を完全化

**パターンプレビュー（slide-deck-builder）**
- パターン割り当て時に `SLIDE-DECK/pattern-preview.html` を自動生成するように
- 各スライドの割り当てパターンと同カテゴリの代替候補2〜3個を、スケルトンHTMLの縮小サムネイルで一覧確認できる
- 「Slide 3 → 候補B」のようにサムネイルのラベルで差し替え指示が可能に
- プレビュー冒頭にパターンギャラリーへのショートカットボタンを配置（候補以外から選びたいときは全127種類を一覧できる）
- パターン名の文字列だけでは構図がイメージできず、ギャラリーと見比べる手間があった問題を解消

---

## [1.2.0] - 2026-07-05

### Added

**Claude Code プラグイン対応**
- `.claude-plugin/`（plugin.json・marketplace.json）を追加し、`/plugin` コマンドだけでインストールできるように
- slide-deck-builder に、プラグイン同梱ファイル（サンプルデザインシステム・スライドパターン）へのフォールバックを追加（カレントディレクトリにファイルがなくても同梱サンプルで動作する）
- プラグイン化とフォールバックの仕組みは [skanehira さんのフォーク slide-plugin](https://github.com/skanehira/slide-plugin) に着想を得ました。ありがとうございます

**新スキル slide-scenario-creator（4つ目のスキル）**
- スライドの中身（シナリオ・構成案）をClaudeとの壁打ちで作成するスキルを追加
- シナリオ作成・レビュー（良い点→改善点→デビルズアドボケイトの3段階）・ペルソナ設定の3モード構成
- 出力：`SLIDE-SCENARIO/SLIDE-SCENARIO-{name}.md`・`PERSONA/PERSONA-{name}.md`

**slide-deck-builder**
- STEP 0 を追加：`SLIDE-SCENARIO-*.md` があればブリーフのヒアリングをスキップし、シナリオの内容から設計書を作成

**スライドパターンの拡充**
- パターンを 99 種類 → 127 種類に拡充（10件＋18件を新規追加）
- フレームなしパターン（`frame: none`）のルールを追加（表紙・セクション区切り・締め等の全面レイアウトに対応）

### Changed

**スライドパターン**
- 旧形式パターン98件を新仕様（1920×1080・0.75倍スケール表示）に統一し、ギャラリーを新旧両対応に
- slide-pattern-creator の再現度を向上（比率・ギャップ・フォントサイズ・角丸の再現ルールを強化）

### Fixed

**スキル間の整合性**
- slide-deck-builder：SLIDE-DECK.md に埋め込むSLIDE.mdのセクション一覧に `Component Style` が抜けていた問題を修正
- slide-deck-builder：出力先を `SLIDE-DECK/SLIDE-DECK-{name}/` に統一（他スキルと同じ2階層フォルダ構成に）
- slide-deck-builder：「目的」の選択肢を slide-scenario-creator と統一（7択に拡張）
- slide-pattern-creator：SLIDE-PATTERN-INDEX.md を新規作成する場合もカテゴリ見出し付きの形式で生成するよう修正（slide-deck-builder のパターン検索がカテゴリに依存するため）

**sample.html 生成仕様の汎用化（slide-md-creator）**
- グラデーション「なし」のデザインに対応（`--gradient-main` / `--gradient-vert` をPrimary単色にする読み替えルールを追加。ソースにないグラデーションを追加しないことを明記）
- ハードコードされていたグレー文字色・カード背景色・枠線色をデザイントークン（`--color-text-muted` / `--color-surface` / `--color-border`）参照に変更し、暗色デザインに対応
- SLIDE.mdで「なし」と定義された装飾（背景アクセント・縦バー・ブランドフッター）はサンプルに適用しないルールを明記
- 未定義の「Successカラー」参照を修正し、Do/Don'tアイコンの色を明示（緑 `#1E8E3E` / 赤 `#D93025`）

---

## [1.1.1] - 2026-06-29

### Added

**サンプルデザインシステムの拡充（docs/SLIDE-md/）**
- サンプルを 4 種類 → 10 種類に拡充
- 新規追加した 6 種類：
  - `SLIDE-md-golden-yellow`（ゴールデンイエロー：BtoBビジネス発表・社内共有）
  - `SLIDE-md-blue-teal-recruitment`（ブルー×ティール：採用・コーポレートブランディング）
  - `SLIDE-md-blue-simple-diagram`（ブルー：教育・研修・図解型プレゼン）
  - `SLIDE-md-green-blue-business`（グリーン×ブルー：営業・サービス提案・BtoB）
  - `SLIDE-md-corporate-red`（コーポレートレッド：決算説明会・IR資料）
  - `SLIDE-md-sky-corporate`（スカイコーポレート：統合報告書・アニュアルレポート）
- 全サンプルのメタ情報（表示名・想定シーン・カラースウォッチ）を管理する `index.json` を追加

### Changed

**既存 4 種類のサンプルを全面改訂**
- `SLIDE-md-MintGreen`・`SLIDE-md-anthropic`・`SLIDE-md-digital`・`SLIDE-md-gemini-color-system` を v1.1.0 の sample.html 共通実装スタイルで作り直し
  - 背景アクセント丸・縦バー・ページ番号・ブランドフッター・カードコンポーネントの標準スタイルを適用
  - スライドサイズ：960 × 540 px → 1920 × 1080 px（0.75 倍スケール表示）に統一

---

## [1.1.0] - 2026-06-28

### Added

**デザイントークンの拡張**
- カラーに `Text Sub`（見出し・ラベル色）と `Text Muted`（補足・キャプション色）を追加
- カラーに `Gradient`（グラデーションの有無・カラーストップ・方向）を追加
- タイポグラフィに `モノスペースフォント（Mono）` を追加（数値・コード・ページ番号に使われるフォント）

**新セクション**
- `Background Accent（背景装飾）`：半透明の装飾円・装飾図形の有無・配置・カラーを定義するセクションを追加
- `Component Style（コンポーネントスタイル）`：カード角丸・影・縦バー・箇条書きマーカー・番号スタイルを定義するセクションを追加

**Slide Frame の拡張**
- ページ番号スタイル（グラデーションテキスト / 単色・モノスペースフォント使用有無）を追加
- ブランドフッター（左下表示・内容）を追加
- 背景アクセント（半透明グラデーション丸の有無・配置・カラー）を追加
- 縦バー（見出し左側の縦バーの有無・スタイル・幅）を追加

**sample.html 共通実装**
- 全ページ共通の装飾要素として、背景アクセント丸・縦バー・ページ番号・ブランドフッター・カードコンポーネントの標準スタイルを追加
- ダミーコンテンツポリシーを策定（会社名は「サンプル株式会社」に統一、本文・数値はすべて汎用ダミーデータを使用）

### Changed

**デザイントークン**
- 見出しフォントの役割ラベルを「見出しフォント」→「見出しフォント（Display）」に変更
- 本文フォントの役割ラベルを「本文フォント」→「本文フォント（Body）」に変更

**sample.html 生成仕様**
- スライド数：5ページ → 6ページ
- スライドサイズ：960 × 540 px → 1920 × 1080 px（ブラウザ上では0.75倍スケール＝1440 × 810 px相当で表示）
- ページ1（仕様書ページ）を全面改訂：カラーパレット・スライドで使うパーツ・タイポグラフィ・レイアウト・Do/Don'tを1画面に集約
- ページ4：箇条書きをグラデーション円の番号付きカード形式に変更
- ページ5：棒グラフ（左）＋ドーナツグラフ（右）の2グラフ構成に変更
- ページ6：3列カード（価値と説明）＋締めのCTAボタン構成に変更
- CSSカスタムプロパティに `--color-text-sub` / `--color-text-muted` / `--gradient-main` / `--gradient-vert` / `--font-mono` 等を追加

---

## [1.0.0] - 2026-01-01

初回リリース。
